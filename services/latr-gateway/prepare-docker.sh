#!/usr/bin/env bash
# Legacy hook kept for deploy.sh; LatrKit now resolves from GitHub in Package.swift / Docker build.
set -euo pipefail
echo "LatrKit resolves from https://github.com/Stygian-Tech/latr-kit (no local vendor staging)."
