# LatrKit (moved)

Canonical **`LatrKit`** source: https://github.com/Stygian-Tech/latr-kit

`services/latr-gateway/Package.swift` depends on it via SwiftPM:

```swift
.package(url: "https://github.com/Stygian-Tech/latr-kit.git", branch: "main")
```

Do not reintroduce an in-tree copy here.
