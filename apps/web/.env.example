# Local OAuth: ATProto requires loopback client id "http://localhost" (no path/port).
NEXT_PUBLIC_APP_ENV=local

# Optional: override hosted client metadata URL (production default is https://latr.link/client-metadata.json)
# NEXT_PUBLIC_ATPROTO_CLIENT_ID=https://latr.link/client-metadata.json
